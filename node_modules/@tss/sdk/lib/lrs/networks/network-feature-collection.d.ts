import { FeatureCollection } from 'geojson';
import { NetworkFeature } from './network-feature';
import { LrsFeatureCollection } from '../lrs-feature-collection';
import { NetworkLayer } from './network-layer';
export declare class NetworkFeatureCollection extends LrsFeatureCollection {
    type: 'FeatureCollection';
    features: Array<NetworkFeature>;
    constructor(collection: FeatureCollection, layer: NetworkLayer, spatialReferenceId?: number);
}
