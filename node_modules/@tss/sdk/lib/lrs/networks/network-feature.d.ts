import { Feature, LineString, MultiLineString } from 'geojson';
import { LrsFeature } from '../lrs-feature';
import { NetworkLayer } from './network-layer';
import { SinglePoint } from '../../geometry/point/single-point';
import { Cloneable } from '../../core/clone/cloneable';
export declare class NetworkFeature extends LrsFeature implements Cloneable<NetworkFeature> {
    layer: NetworkLayer;
    constructor(feature: Feature, layer: NetworkLayer, spatialReferenceId?: number);
    static create(layer: NetworkLayer, properties?: {
        [name: string]: any;
    }, geometry?: LineString | MultiLineString): NetworkFeature;
    getMeasure(point: SinglePoint): number;
    getPoint(measure: number): SinglePoint;
    clone(): NetworkFeature;
}
