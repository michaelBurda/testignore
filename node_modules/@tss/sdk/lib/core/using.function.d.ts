import { Disposable } from './disposable';
/**
 * Provides a syntax that ensures the correct use of Disposable objects.
 */
export declare function using<T extends Disposable>(resource: T, func: (resource: T) => Promise<void> | void): Promise<void>;
