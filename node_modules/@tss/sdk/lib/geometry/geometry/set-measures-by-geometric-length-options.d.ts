import { CalibrationPosition } from '../line/calibration/calibration-point';
export interface SetMeasuresByGeometricLengthOptions {
    units?: 'miles' | 'kilometers' | 'radians' | 'degrees';
    hasZValues?: boolean;
    calibrationPositions?: CalibrationPosition[];
}
