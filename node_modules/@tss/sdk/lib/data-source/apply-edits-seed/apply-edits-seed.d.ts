import { Feature } from 'geojson';
export interface ApplyEditsSeed {
    dataSourceId: string;
    tableName: string;
    idFieldName: string;
    addIfNotExists?: boolean;
    addOrUpdate?: boolean;
    adds: Feature[];
    updates: Feature[];
    deletes: Feature[];
}
