import { ApplyEditsError } from './apply-edits-error';
import { ApplyEditsInsertLog } from './apply-edits-insert-log';
export interface ApplyEditsResult {
    addCount: number;
    updateCount: number;
    deleteCount: number;
    successfulTransactionCount: number;
    elapsedSeconds: number;
    hasErrors: boolean;
    errors: ApplyEditsError[];
    recordInsertLog: ApplyEditsInsertLog[];
}
