import { Observable } from 'rxjs';
/**
 * Allows for methods to add and remove items from a queue.
 */
export interface Queuable<TInput> {
    enqueue(item: TInput): Observable<TInput>;
    dequeue(item: TInput): Observable<TInput>;
}
